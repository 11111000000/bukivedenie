# Минимальные зависимости для Termux (Android), без тяжёлых и C-сборок
# Цель — запустить analyze_text.py и базовый конвейер извлечения статистики

# Разбиение/токенизация
razdel>=0.3.0
regex>=2023.10.3

# CLI
typer>=0.9.0

# Морфология RU (без C++ dawg; используем чисто-питоновский пакет)
dawg-python>=0.7.2
pymorphy2>=0.9.1
pymorphy2-dicts-ru>=2.4.417127.4579844

# Тональность (EN, опционально)
vaderSentiment>=3.3.2

# Утилиты
tqdm>=4.65.0
